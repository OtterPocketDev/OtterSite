{
  "build": {
    "image": "node:12.14.0",
    "command": "npm install && npm run build && echo $SOME_ENV",
    "publicDir": "dist",
    "baseDir": "/",
    "environment": {
      "SOME_ENV": "Build finished! Party!"
    }
  }
}
